spring:
  cloud:
    config:
      uri: http://localhost:20000
      name: application
      profile: back,eureka,thymeleaf
feign:
  client:
    config:
      default:
        connectTimeout: 4000
        readTimeout: 4000